[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "dharmatiles"
version = "0.1.0"
description = "Procedural 3D-printable terrain tiles"
requires-python = ">=3.11"
dependencies = [
    "numpy",
    "trimesh",
    "scipy",
    "pyyaml",
    "manifold3d",
    "rich",
]

[project.scripts]
generate-tile-stl = "dharmatiles.terrains.tile:main"

[tool.setuptools.packages.find]
where = ["src"]
